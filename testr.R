#ceci est un test

#fonction qui renvoie la somme de deux nombres
somme <- function(x,y)
  {return(x+y)}

#fonction qui renvoie la différence entre deux nombres
différence <- function(x,y)
  {return(x-y)}
  
